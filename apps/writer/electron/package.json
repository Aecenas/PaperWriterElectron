{
  "name": "paperwriter-electron",
  "version": "0.9.2",
  "private": false,
  "main": "main.cjs",
  "scripts": {
    "dev": "electron .",
    "dev:vite": "node dev-vite.cjs",
    "check": "node --check main.cjs && node --check preload.cjs && node --check ai-provider-core.cjs && node --check ai-config-security.cjs && node --check asset-packager.cjs && node --check document-assets.cjs && node --check document-storage.cjs && node --check filesystem-access.cjs && node --check codex-cli-provider.cjs && node --check codex-scope.cjs && node --check codex-image-attachments.cjs",
    "test": "node --test *.test.cjs",
    "dist": "electron-builder --win nsis --x64",
    "pack": "electron-builder --win dir --x64",
    "publish": "electron-builder --win nsis --x64 --publish always"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "electron": "^41.5.0",
    "electron-builder": "^26.15.3"
  },
  "productName": "笺间",
  "description": "面向个人长文、日记、复盘与资料整理的沉浸式 Windows 写作软件",
  "author": "Aecenas",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Aecenas/PaperWriterElectron.git"
  },
  "homepage": "https://github.com/Aecenas/PaperWriterElectron#readme",
  "build": {
    "appId": "com.aecenas.paperwriter",
    "productName": "笺间",
    "asar": true,
    "electronFuses": {
      "runAsNode": false,
      "enableCookieEncryption": true,
      "enableNodeOptionsEnvironmentVariable": false,
      "enableNodeCliInspectArguments": false,
      "enableEmbeddedAsarIntegrityValidation": true,
      "onlyLoadAppFromAsar": true
    },
    "artifactName": "PaperWriter-${version}-${arch}.${ext}",
    "directories": {
      "output": "../../release"
    },
    "files": [
      "**/*",
      {
        "from": "../frontend/dist",
        "to": "frontend/dist",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/app-icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "笺间"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "Aecenas",
        "repo": "PaperWriterElectron"
      }
    ]
  }
}
