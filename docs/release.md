# 发布与维护

本文档记录 `1.1.7` 版本的发布准备流程。

## 版本号位置

发布前需要保持以下文件版本一致：

- `apps/writer/frontend/package.json`
- `apps/writer/frontend/package-lock.json`
- `apps/writer/frontend/src/release-notes.js`（首条记录需对应当前版本）
- `apps/writer/electron/package.json`
- `apps/writer/electron/package-lock.json`
- `README.md`

当前目标版本：`1.1.7`

发布新版本时，还应在 `release-notes.js` 顶部新增发布日期、标题和面向用户的更新摘要，确保应用内版本号弹窗可以离线查看完整历史。

## 1.1.7 发布摘要

- 新增 Qwen、Kimi、智谱 GLM、OpenAI、Claude 和 OpenRouter 六个不可删除的内置服务商预设，并为设置页和模型选择器加入各家官方图标。
- 按官方当前接口校准默认协议、Base URL 与模型；Qwen 默认使用 `qwen3.7-plus`，Kimi 使用与现有多轮记录兼容的 `kimi-k2.6`，OpenAI 使用均衡档 `gpt-5.6-terra`，Claude 使用原生 Messages API。
- 加固 AI 请求目标、重定向与日志隐私边界，降低内网探测、凭据跨站发送和诊断信息泄露风险。
- 修复 Mermaid 安全渲染在编辑器、分页预览及 PDF、图片、DOCX 导出链路中的兼容问题，并收紧图片与文档资源限额。
- 改善窗口无响应与渲染进程异常恢复提示，补充包体预算、自动化回归和安装包启动检查。

## 1.1.6 发布摘要

- 修复 Codex CLI 模型“设为默认”和逐模型推理强度无法保存、点击后被刷新覆盖的问题；本地 CLI 的展示说明不再作为 Base URL 提交。
- 主进程对 Codex CLI 强制忽略 HTTP 地址与 API Key 复用逻辑，保存模型选择时不会再错误清除模型可用状态。
- AI 配置操作新增就地成功或失败反馈，保存结果无需等待刷新即可确认。
- 顶部“设置”改为轻量菜单，可直接进入 AI 配置或模板配置；写作检查仍从结构区进入，配置备份与迁移仍由“出入”菜单管理。
- 同步更新帮助文档、AI精灵离线知识索引和设置入口浏览器回归测试。

## 1.1.5 发布摘要

- 将完整信笺“AI 问答”升级为“AI 协作”，新增意图路由、工作区按需只读检索、实际来源清单、结构化修改方案、逐项审阅、来源锁定、安全版本和派生信笺两阶段提交；普通问答与选区问答保持原有行为和隔离边界。
- 新增“帮助 → AI精灵”，以独立本机多会话、版本化内置知识检索和专用任务模型回答笺间使用问题，支持停止、重试、复制和跳转帮助来源，且不读取信笺、文件路径或资料区。
- 新增资料翻译，可临时翻译 PDF 当前页以及 DOCX、Markdown、TXT、LOG、CSV、TSV 预览；保留页面或文档结构，不修改源文件，并提供分批校验、取消恢复和仅本次运行有效的内存缓存。
- AI 设置新增 AI精灵、资料翻译两个任务模型；旧配置自动补齐，显式指定模型失效时不会静默回退。
- 优化 AI 协作的请求体积、阶段耗时提示和空操作容错，避免模型夹带空修改时重复修复并拖垮整份方案。
- 图片悬浮工具新增直接删除入口并精简图号引用；PDF 与分页图片导出延续全文图片和 Mermaid 图编号，并修复跨页段落重复首行缩进。
- 帮助中心保持 29 个主题，补齐 AI 协作、资料翻译、AI精灵、任务模型与隐私边界；帮助主题和 AI精灵检索索引改由同一版本化知识源生成并在构建时校验。

## 1.1.1 发布摘要

- 修复 PDF 导出时目录条目被误删的问题，完整保留目录编号、层级和标题。
- 修复 Electron 设备缩放产生的亚像素分页误差，避免页首图片被归入上一页后裁掉，并消除对应的大块空白。
- 图片按编辑器中的真实页位和内容顺序导出，不为追求页面填充而前移或后移。
- 补充 PDF 静态语义内容、块级图片分页边界和亚像素列起点回归测试。

## 1.1.0 发布摘要

- 开启“专业创作与全稿工作流”阶段，`1.1.0` 为该阶段首个大版本。
- 新增 AI 起稿，可从写作简报和明确选择的参考资料依次完成结构设计、分节撰写、全文检查并生成不覆盖原稿的新信笺。
- 新增 Emoji、段落书签、代码块、TeX 行内与块公式、公式编号和引用，以及支持实时预览、图注和宽度设置的 Mermaid 图。
- 新增本地中英文写作检查、自定义白名单与用词规范，并支持定位、忽略、替换和全文替换。
- 文献库升级为私域 / 公域双层管理，支持 BibTeX、RIS、CSL-JSON、常用引用样式和 DOI / ISBN 联网补全。
- 新增连续、单页与双页视图、四级标题和可折叠编号大纲。
- 新增本地信笺版本历史、差异对照、安全恢复，以及 AI 应用前保护版本。
- 新增 `.jianprofile` 配置备份与迁移，提供完整性校验、差异预览和可选的 API Key 口令加密。
- 更新应用内帮助中心至 29 个主题，并补齐新版界面配图。

## 1.0.0 发布摘要

- 顶部搜索新增“资料搜索”，覆盖当前资料区文件、可解析正文，以及公区和当前工作区私区网页的标题与网址，并提供大型索引进度和取消操作。
- PDF、DOCX 等正文提取由独立 worker 执行，并加入版本化缓存、增量失效、容量上限和损坏文件隔离。
- 更新下载状态栏改为确定型或准备态进度环，显示百分比、已下载量和速度，减少重复 toast。
- 正文选区工具栏新增按信笺归属的多会话 AI 问答；窗口可拖动、切换会话并最小化为右下角 AI 小精灵继续后台生成，请求仍只包含冻结选区、问题和当前会话历史。
- AI 任务模型改为默认折叠的独立卡片，可同时展开多项并保留参数草稿；“选区问答”可跟随默认模型或精确指定已测试模型。
- 完成前端、Electron 与平台桥的渐进式模块化拆分，保持既有 UI/UX、快捷键、文件格式和外部 API 兼容。

## 0.9.11 发布摘要

- 资料区新增 DOCX 文件直接打开与阅读，沿用现有的压缩包预检、图片限额和 HTML 安全清洗。
- 修复 AI 取消后的迟到事件、双编辑组导出目标和未来格式只读保护问题。
- 修复文件树、资料树和目录监听的异步竞态，旧请求不会再覆盖当前目录。
- 保存、恢复缓存和外部修改状态改为按标签独立管理，并修复保存期间继续编辑的状态误判。
- 浏览器存储异常不再伪报成功，补充可恢复错误页、弹窗焦点与快捷键隔离。
- 新增真实浏览器交互回归测试，覆盖关键弹窗、导出、只读、标签状态和 AI 取消流程。

## 0.9.10 发布摘要

- 修复分页图片导出时，正文插图偶尔只显示边框和标题的问题。
- 导出会等待原图完成加载与解码；失败或超时时明确报错，不再写出空白图片。

## 0.9.9 发布摘要

- 修复普通文字粘贴时被误识别为网页链接的问题。
- AI 优化结果改用桌面端原生剪贴板复制，修复写入权限错误。
- 图片尺寸选项改为居中显示。
- 修复分屏模式下导航栏下拉框被标签栏遮挡的问题。

## 0.9.8 发布摘要

`0.9.8` 汇总自 `0.9.2` 以来尚未向 GitHub 发布的全部改动，不再保留中间的 `0.9.3`—`0.9.7` 发布记录。

- 新增顶部搜索入口、Ctrl+F 当前信笺搜索和 Ctrl+P 当前文件夹递归全文搜索，并加入 F11 真全屏沉浸模式与分层 Esc 退出。
- 保存加入磁盘 revision 校验、冲突副本、递归监听、窗口聚焦复核，以及恢复缓存和工作区写回的双层持久化。
- 新增 Markdown、HTML、TXT、DOCX 导入，以及 PDF、分页图片、DOCX、Markdown、HTML、TXT 导出，并加固资源和侧车目录安全。
- AI 优化新增安全的“直接应用”，独立裁决链路校验落点与文档状态；定位失败时可手动点选原文，并支持一次撤销。
- 新增独立资料区、稳定资料库身份、文件与网页来源；本机绝对路径不进入同步数据，研究资料始终与 AI 请求隔离。
- 新增自由文本脚注、结构化文献引用、动态编号、自动参考文献，以及关联笺记的反向链接、失效重联和重复身份检测。
- `.letterpaper` 升级到 schema v2；旧文档按需迁移并保留备份，未知未来格式以只读方式打开。
- 写作区升级为左右两个独立编辑组，支持标签压缩与滚动、组内排序、跨组移动，以及双组布局和阅读状态恢复。
- 文档分屏、沙箱网页与资料阅读统一进入右侧阅读位；PDF 阅读器支持紧凑工具栏、搜索、缩放、键盘翻页和阅读位置记忆。
- 左栏重组为“文件 / 资料 / 结构”；关联笺记、脚注、引用和参考文献统一从“元素”菜单插入，结构页专注于查看与管理。

## 0.9.2 发布摘要

- 修复 5000+ 字、多图文档连续输入卡顿：移除逐字 HTML/JSON 序列化和应用根组件更新，改由 ProseMirror 派生状态插件增量提供统计、大纲与目录状态。
- 图片改为无损暂存资源协议，保留 GIF、透明度和原始字节；保存、恢复、AI 原图附件与旧 Data URL 共用安全资源读取链路，并增加 SHA-256 完整性校验。
- 修复自动保存、另存为、多标签关闭、恢复文件清理及文件重命名、移动、删除之间的竞态，避免错误覆盖、误标已保存或丢失最新编辑。
- 加固 Electron IPC、文件系统能力、CSP、ASAR 与生产 fuses，并防护 ZIP bomb、路径穿越、资源伪造、缓存残留和超量解压。
- 加固 AI 配置密钥存储、重定向、响应限量和连接测试竞态；Codex CLI 默认运行在隔离临时目录且只接收受控正文与附件。
- 日常启动默认使用生产构建，开发热更新迁移到显式 `PaperWriter-Dev.cmd` 入口。
- 性能基准中，5260 字与 10 张约 2.1 MB 图片连续输入 P95 为 16.3–16.5 ms，未出现 50 ms 以上长任务。

## 0.9.1 发布摘要

- 修复供应商连接测试成功后，设置页因前端全局状态未同步而立即重新显示“未连接”的问题。
- 加固 Codex CLI 状态刷新：刷新完成前重新读取最新 AI 配置，只合并 Codex 自身状态，避免覆盖同时完成的 Gemini、DeepSeek 或自定义供应商配置。
- Codex 模型目录刷新时保留用户最新保存的推理强度，并补充对应回归测试。

## 0.9.0 发布摘要

- 全面重构左侧工作区、文件树与大纲，补充文件拖动、应用内输入确认、标签阅读位置恢复和打开队列。
- 完善标题编号、正文目录、链接、文档评注、图片标题、音视频、表格、分页图片和 PDF 导出。
- 开放动态 AI 供应商，可添加多个 OpenAI 兼容或 Anthropic 原生接口，并完善模型测试、默认项保护与安全密钥展示。
- 新增 Codex CLI 内置供应商，复用本机登录态，支持模型目录、逐模型推理强度、AI 优化和 AI 问答。
- Codex 问答新增只读目录范围，以及信笺原图/仅标题模式；范围、图片模式和问答记录均按信笺保存。
- 新增用户模板、模板分组、字体排版、高级选项和新建信笺默认模板。
- 全面更新应用内帮助中心至 17 个主题和当前界面配图，并支持单击放大、再次单击缩回。
- 更新应用品牌资源、信纸素材、状态栏、缓存和版本更新反馈。

## 0.8.2 发布摘要

- 修复带编号一级标题末尾右侧无法直接点击放置光标的问题。
- 修复多个信笺标签页切换后不会回到各自上次阅读位置的问题。

## 0.8.1 发布摘要

- 左侧文件树的新建、重命名和删除流程改为应用内弹窗，删除确认不再使用系统原生确认框。
- 文件夹双击进入增加 300ms 识别上限，避免两次间隔较久的单击触发进入。
- 底部“检查更新”在已是最新、错误、开发版或浏览器预览状态后会自动回到可检查状态。
- 正文目录增加层级化视觉样式，并将“目录”标题替换为居中的透明签名图。

## 发布前检查

首次在一台机器上执行发布门禁前，安装 Playwright Chromium：

```powershell
cd apps\writer\frontend
npm run test:e2e:install
```

随后从 Electron 目录执行统一发布门禁：

```powershell
cd apps\writer\electron
npm run release:prepare
```

`release:prepare` 会依次执行 Electron 语法与生成物检查、Electron 单元/契约测试、递归前端单元测试、生产构建、bundle budget、Playwright E2E、真实 Electron preload/IPC 与关闭握手 smoke；`pack`、`dist`、`publish` 均不能绕过该门禁。`dist` 显式禁用 electron-builder 的 tag 自动发布，只有 `publish` 命令可以直接上传。

最后执行启动器检查：

```powershell
cd ..\..\..
.\scripts\Test-Launch-PaperWriter.ps1 -Smoke
```

启动器 smoke 会校验 PowerShell 语法、安全清理约束、npm 依赖和生产前端构建完整性，但不会启动或结束 Electron/Vite。真实 Electron smoke 由 `release:prepare` 负责，并使用一次性 `userData`；正式发布前仍应手工确认一次生产单实例唤起和显式开发入口。

若需要检查实际打包应用，先生成解包产物，再运行 packaged smoke：

```powershell
cd apps\writer\electron
npm run pack
npm run test:packaged-smoke
```

packaged smoke 在隔离的 `%APPDATA%` 下启动 `apps\release\win-unpacked\笺间.exe`，验证 ASAR 页面和真实 preload IPC。打包白名单只包含运行时根 CJS、Electron 资源、前端 `dist` 和生成的知识索引；测试、preload 源码、构建脚本和 source map 不进入 ASAR。

本地生成安装包：

```powershell
cd apps\writer\electron
npm run dist
```

安装包输出到：

```text
apps\release
```

## GitHub Release 准备

1. 确认工作区只包含本次发布需要的改动。
2. 确认 `release:prepare`、启动器 smoke、`pack` 和 packaged smoke 通过。
3. 提交代码。
4. 创建版本标签，例如 `v1.1.7`。
5. 确认 Release provenance workflow 生成安装包、CycloneDX SBOM 和 build provenance attestation。
6. 使用 GitHub Release 发布安装包。

如使用 electron-builder 自动发布，需要配置 GitHub 发布权限，然后执行：

```powershell
cd apps\writer\electron
npm run publish
```

仓库中的 CI 会在 push/PR 上运行 Windows 质量门禁、`moderate` 级生产依赖审计与依赖变更审查，并由 CodeQL 定期扫描；Dependabot 每周检查两个 npm workspace 和 GitHub Actions。版本标签触发的 provenance workflow 会在上传前运行同一发布门禁与 packaged smoke。

Windows 代码签名证书和受保护的签名凭据不属于仓库内实现。无签名本地包可用于内部验证；公开分发前应将签名作为外部发布条件配置并验证，不能把 SBOM 或 provenance attestation 当作代码签名的替代品。

## 更新检查

应用内更新依赖 GitHub Release。发布新版本后，客户端会通过配置的 GitHub 仓库检查更新：

```json
{
  "provider": "github",
  "owner": "Aecenas",
  "repo": "PaperWriterElectron"
}
```

发布后建议安装旧版本并执行一次“检查更新”，确认更新状态和安装包下载链路正常。
